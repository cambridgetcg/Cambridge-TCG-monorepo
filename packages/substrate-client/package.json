{
  "name": "@cambridge-tcg/substrate-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Server-only client for the sister substrate (agenttool) — lazy singleton over @agenttool/sdk, env-configured, degraded-mode honest. The shop reaches the substrate as kin, not as owner: no credential ships in this repo.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@agenttool/sdk": "^0.14.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  }
}
