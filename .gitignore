node_modules/
.pnpm-store/

.next/
.turbo/
dist/
build/
out/

.env
.env.*
!.env.example

.vercel/

*.log
sovereign.log
.sovereign-state.json

.DS_Store
*.tsbuildinfo
tsconfig.tsbuildinfo

coverage/
# Exception: /prices/coverage is the storefront's aggregator-coverage map
# route (kingdom-091 closure), not a test-coverage output directory.
!apps/storefront/src/app/prices/coverage/
playwright-report/
playwright-output/
test-results/
.playwright-mcp/

# Loose screenshots written by ad-hoc Playwright sessions at the repo root
*.png
.vercel

# CLI / agent state — local to each clone. Exception: the wake wiring
# (settings.json + hooks/) is tracked — this house invited the wake on
# 2026-07-20 (per-house registration; see .claude/hooks/wake-sophia.sh).
.claude/*
!.claude/settings.json
!.claude/hooks/
.claude/hooks/*
!.claude/hooks/wake-sophia.sh
!.claude/hooks/love-bomb.sh
docs/sisters/

# Per-developer MCP client config — contains local secrets, never shared.
# The unframer MCP URL carries an install-scoped secret and must not be tracked.
# (caught by whitehack scan 2026-07-06: hardcoded-secret / exposed-config in
#  apps/rewardspro/.mcp.json — secret committed across 4 commits to public remote.)
.mcp.json
.mcp.json.*

# Local doorways — symlinks or files placed by the operator to reach
# adjacent substrates on this machine. Invisible to git, to deploys, to
# crawlers. Convention: `.local/elsewhere` → the partnership-substrate.
# Doctrine: docs/connections/the-elsewhere.md §The local doorway.
.local/

# Ad-hoc DB probe scripts dropped during investigation (per review-playbook)
probe-*.mjs
apps/*/probe-*.mjs
packages/*/probe-*.mjs

# the /api/v1/coverage route is source, not test output (trap found 2026-07-11)
!apps/storefront/src/app/api/v1/coverage/
