import { ComingSoon } from "@/lib/admin/ui";
export const metadata = { title: "KYC Verifications" };
export default function Page() {
  return (
    <ComingSoon
      title="KYC Verifications"
      description="Identity document review — approve or reject."
      missionId="kingdom-025"
      operatingFromUrl="https://cambridgetcg.com/admin/verifications"
    />
  );
}
