name = "remix"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
predev = "npx prisma generate"
# Removed "npx prisma migrate deploy" - using Aurora Data API (no direct connections)
# Migrations are handled separately via: npm run migrate:data-api
dev = "npm exec remix vite:dev"
