import { ComingSoon } from "@/components/layout/ComingSoon";
export const metadata = { title: "Cron Jobs" };
export default function Page() {
  return <ComingSoon title="Cron Jobs" description="Health dashboard, run history, and failure alerts for all 42 scheduled jobs across both apps. Zero visibility today." />;
}
