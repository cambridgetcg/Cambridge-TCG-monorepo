import { ComingSoon } from "@/components/layout/ComingSoon";
export const metadata = { title: "Users" };
export default function Page() {
  return <ComingSoon title="Users" description="Storefront user search, profile detail, role management, and suspension. This is one of the most critical missing features — impossible to do today." />;
}
